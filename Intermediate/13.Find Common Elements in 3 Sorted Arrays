Find Common Elements in 3 Sorted Arrays | Find common elements in three sorted arrays.

import java.util.*;
class Main {
    public static void main(String[] args) {
        int[] arr1 = {1, 5, 10, 20, 40, 80};
        int[] arr2 = {6, 7, 20, 80, 100};
        int[] arr3 = {3, 4, 15, 20, 30, 70, 80, 120};
        System.out.println("Common elements in an arrays: "+ commonElements(arr1,arr2,arr3));
    }
    public static Set<Integer> commonElements(int[] arr1,int[] arr2,int[] arr3){
        Set<Integer> hs = new HashSet<>();
        Set<Integer> hs1 = new HashSet<>();
        Set<Integer> result = new HashSet<>();
        for(int num : arr1){
            hs.add(num);
        }
        for(int num : arr2){
            if(hs.contains(num)){
                hs1.add(num);
            }
        }
        for(int num : arr3){
            if(hs1.contains(num)){
                result.add(num);
            }
        }
        return result;
    }
}

output:
Common elements in an arrays: [80, 20]
