Convert String to Integer (Custom) | Implement a function to convert a string to an integer without using `Integer.parseInt()`. |

import java.util.*;
class Main {
    public static void main(String[] args) {
        String str = "12345";
        isNumeric(str);
    }
    public static void isNumeric(String str){
        int res = 0;
        for(int i=0;i<str.length();i++){
            char ch = str.charAt(i);
            if(!(Character.isDigit(ch))){
                continue;                             // If it's not a digit, skip (or you could return an error)
            }
            int digit = ch-'0';                      // Convert char to numeric digit
            res = res * 10 + digit;                  // Shift left and add new digit
        }
        System.out.println(res);
    }
}

output:
12345
