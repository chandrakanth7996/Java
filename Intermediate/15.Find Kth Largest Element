Find Kth Largest Element | Find the Kth largest element in an unsorted array. |

import java.util.*;
class Main {
    public static void main(String[] args) {
        int[] arr = {7, 10, 4, 3, 20, 15};
        int k = 3;
        System.out.println("k largest element : "+ kLargest(arr,k));
    }
    public static int kLargest(int[] arr,int k){
        Arrays.sort(arr);
        System.out.println(Arrays.toString(arr));
        return arr[arr.length-k];
    }
}

output:
[3, 4, 7, 10, 15, 20]
k largest element : 10
