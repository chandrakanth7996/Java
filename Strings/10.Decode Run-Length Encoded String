Decode Run-Length Encoded String | Decode a given run-length encoded string. |

import java.util.*;
class Main {
    public static void main(String[] args) {
        String str = "a3b4";
        StringBuilder s = new StringBuilder();
        for(int i=0;i<str.length();i+=2){
            char ch = str.charAt(i);
            int count = str.charAt(i+1)-'0';                  // Convert char digit to int
            s.append(String.valueOf(ch).repeat(count));
        }
        System.out.println(s.toString());
    }
}

output:
aaabbbb
