Write a program that takes an array of integers and counts how many of them are positive numbers (greater than 0).
Example
Input: {3, -1, 4, -2, 0, 6}
Output: 3 (since there are three positive numbers: 3, 4, and 6)

import java.util.*;
class HelloWorld {
    public static void main(String[] args) {
        int[] arr = {3, -1, 4, -2, 0, 6};
        int n = arr.length;
        int count=0;
        for(int i=0;i<n;i++){
            if(arr[i]>0){
                count++;
            }
        }
        System.out.println(count);
    }
}
