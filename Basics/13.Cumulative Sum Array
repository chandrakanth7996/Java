Cumulative Sum Array
Problem: Given an array of integers, write a function that returns an array where each element at index i is the sum of elements from the start of the array up to i.
For example:
Input: [1, 2, 3, 4]
Output: [1, 3, 6, 10]

import java.util.*;
class HelloWorld {
    public static void main(String[] args) {
        int[] arr = {1,2,3,4};
        int n = arr.length;
        int sum=0;
        List<Integer> list = new ArrayList<>();
        for(int i=0;i<n;i++){
            sum += arr[i];
            list.add(sum);
        }
        System.out.println(list);
    }
}

summary: 
Find the sum of the array and store the result into list.
